uniform sampler2D texture;
uniform vec2 size;
uniform vec4 color;

in vec2 modelPos;

void main()
{
	if(distance(modelPos, vec2(0.5)) > 0.5)
		discard;
	gl_FragColor = vec4(color.x, color.y, color.z, pow(1 - distance(modelPos, vec2(0.5)) * 2, 2));
}